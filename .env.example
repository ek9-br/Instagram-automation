# Configuração do front-end (Vite). Copie para `.env` e preencha.
# Apenas chaves públicas (anon) — nunca coloque service_role aqui.

# URL do projeto Supabase (ex.: https://xxxx.supabase.co)
VITE_SUPABASE_URL=

# Anon/public key (Project Settings → API)
VITE_SUPABASE_ANON_KEY=
